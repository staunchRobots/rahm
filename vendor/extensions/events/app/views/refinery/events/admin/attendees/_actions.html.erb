<ul>
  <% if ::Refinery::Events::Admin::AttendeesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.events_admin_attendees_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_events_admin_attendee_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Events::Admin::AttendeesController.sortable? && ::Refinery::Events::Attendee.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Attendees"),
                 refinery.events_admin_attendees_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Attendees"),
                 refinery.events_admin_attendees_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
