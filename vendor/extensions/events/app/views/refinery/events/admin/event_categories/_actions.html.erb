<ul>
  <% if ::Refinery::Events::Admin::EventCategoriesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.events_admin_event_categories_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_events_admin_event_category_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Events::Admin::EventCategoriesController.sortable? && ::Refinery::Events::EventCategory.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Event Categories"),
                 refinery.events_admin_event_categories_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Event Categories"),
                 refinery.events_admin_event_categories_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
